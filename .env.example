# OAuth Configuration
# Set to 'true' when OAuth credentials are properly configured in backend
VITE_OAUTH_CONFIGURED=false

# API Base URL (defaults to relative path /api)
# VITE_API_BASE=http://localhost:8080/api